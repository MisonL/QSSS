# AGENTS.md - QSSS 仓库执行约束

## 1. 基本原则

- 全程使用中文沟通，结论必须基于代码、测试、日志或 git 证据。
- 当前仓库是个人自用 A 股行情、板块、资金流分析系统，不另起炉灶。
- 免费优先数据源顺序：pytdx 实时行情，AKShare 板块和资金流，BaoStock 历史和复权，同花顺本地缓存成分。
- 不逆向同花顺私有 socket 作为主数据源，不复用 WebKit 缓存中的登录态 URL。
- 禁止为“先跑通”添加隐藏回退、静默容错、mock 成功路径或吞异常继续。

## 2. 任务源和工作流

- `issues.csv` 是唯一任务驱动源。临时计划和聊天记录只做参考。
- 每次只处理一个原子任务，流程为：读取任务，锁定为 `进行中`，开发，验证，自审，标记 `已完成`，再处理下一个任务。
- 任务状态只允许：`未开始`、`进行中`、`已完成`。
- 若 `issues.csv` 与代码事实冲突，优先级为：`issues.csv` 状态，git 提交证据，当前代码实现。
- 标题包含 `[Code Review]` 的任务只做审计报告和后续修复任务，默认不改业务代码。

## 3. 开发边界

- 先保证核心逻辑输入输出行为等价，再考虑性能优化、分布式和 ML。
- 只修改当前任务需要的文件，不顺手重构无关模块。
- 代码中不得硬编码密钥、token、cookie 或本机私有登录态。
- 外部输入必须在边界处校验，数据库访问必须参数化。
- 修改 Rust/Python 扩展或本地编译产物后，必须确认运行环境实际加载的是新版本。

## 4. 代码和文档卫生

- 代码、注释、日志、Markdown 文档不得新增 emoji、Unicode 装饰符号或装饰性框线。
- 使用普通 Markdown 列表、标题和 ASCII 表格表达结构。
- 注释只在非直观逻辑处添加，避免复述代码。
- 默认不提交缓存、日志、虚拟环境、临时文件和个人配置。

## 5. 测试与验证

- 默认测试命令：
  ```bash
  uv run --with pytest --with pandas --with numpy --with pytdx --with pydantic --with pydantic-settings --with loguru --with click --with scikit-learn --with lightgbm pytest -q
  ```
- 外部数据源测试必须标记 `external`，默认测试不得依赖真实上游服务。
- macOS 若访问真实 LightGBM 训练路径失败并提示 `libomp.dylib` 缺失，按真实环境问题记录，不得伪造通过。
- 每个原子任务完成前必须记录验证命令、退出码和摘要。

## 6. 当前阶段重点

- 先稳定数据源契约、行情/板块/资金流采集和 Web 展示。
- 暂不优先推进复杂 ML、分布式优化或大规模回测。
- pytdx 字段契约、AKShare 板块资金流接入、同花顺本地板块缓存解析、SQLite 落库和 Web MVP 是当前优先方向。
